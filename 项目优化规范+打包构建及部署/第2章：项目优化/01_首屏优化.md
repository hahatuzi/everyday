# 一：导致首屏加载慢的原因以及优化策略
  ## 1.资源太大
  - 分包
  - 懒加载
  - 公共资源vender
  - 缓存：强缓存，协商缓存，策略缓存
  - 服务端渲染
  - 局部SSR(C端落地页，广告页)
  ## 2.网络延迟
  - cdn
  - preload
  - prerender

# 二：优化衡量指标
  ## 1.FP(performance API)
  ## 2.FCP(performance API)
  ## 3.FMP(mutationObserver API)
  ## 4.LCP(mutationObserver API)

# 三：性能监听以及上报
  ## 1.性能监听
  ## 2.用户行为采集：无痕埋点，手动埋点，可视化埋点
  ## 3.异常采集：错误边界，异常捕获

# 四：首屏加载方式
  ## 1.网络优化
  - （1）减少HTTP请求
  - （2）合理设置服务端缓存，提高服务器处理速度。（强制缓存：Expires/cache-Control，对比缓存:Etag/if-none-match/last-modified/if-modified-since）http优化：协商缓存和强缓存,强缓存：服务端给响应头追加一些字段，客户端会记住这些字段，在expires(截止失效时间)没有到达之前，无论你怎么刷新页面浏览器都不会重新请求页面，而是从缓存中取协商缓存：是否使用缓存要和后端商量，当服务端给我们打上协商缓存的标记后，客户端在下次刷新页面需要重新请求资源的时候发送请求给服务器，如果内容需要改变，就会响应，没有变化就会响应304。
  - （3）使用**dns-prefetch**进行DNS预解析，<link dns-prefetch>
  - （4）采用**域名分片**技术，将资源放在不同的域名下，解除同一个域名最多处理6个TCP链接问题
  - （5）采用**CDN加速**,就近服务
  - （6）**gzip压缩**优化，对传输资源进行体积压缩

  ## 2.静态文件优化
  - （1）图片压缩比如tinypng工具,精灵图，图片格式，webP(**同等质量体积小30%**)
  - （2）减少iframe使用
  - （3）CSS外联

  ## 3.组件按需加载
  - react suspense,lazy

  ## 4.延迟加载
  - 滚动加载，可识区域加载
  - 通过**async**,**defer**异步加载js文件

  ## 5.预加载preload
  - （1）加载数据优先级，**preload:预先请求当前页面需要的资源**，**prefetch:未来页面中使用的资源**,<link rel="preload" href="style.css">，比如**首页的内容都使用preload**,子页面用prefetch
  ## 6.预渲染
  - prerender

# 五：浏览器指标详解
|   时间节点  |              描述              |                         含义                            |
|:----------:| :----------------------------:|:------------------------------------------------------:|
|     TTFB   |       time to first byte      |               从请求到数据返回第一个字节所消耗的时间          |
|     TTI    |      time to interactive      |                DOM树构建完毕，代表可以绑定事件              |
|     DCL    |         domContentLoaded      |   当**html文档被完全加载和解析完成**后触发domContentLoaded   |
|      L     |               onLoad          |                  **全部依赖资源加载完毕**后触发             |
|     FP     |            first paint        |                 首次绘制，第一个像素点绘制到屏幕的时间        |
|     FCP    |    first contentful Paint     |                       首次绘制任何内容的时间               |
|     FMP    |    first meaningful paint     |             首次有意义的绘制，它是衡量页面可用性的标准         |
|     LCP    |   largest contentfule paint   |               viewport中最大的页面元素加载的时间            |
|     FID    |         first input delay     |               用户首次和页面交互到页面响应的时间             |

# 六：备注
[!参考文章-前端性能优化]https://juejin.cn/post/7214026775142760505
[!前端性能优化大全]https://juejin.cn/post/7468655596641976320#heading-78
lightHouse:查看网页优化性能
[!lighthouse下载地址]https://chrome.google.com/webstore/detail/lighthouse/blipmdconlkpinefehnmjammfjpmpbjk/related
npm i lightHouse -G
lightHouse http://www.taobao.com