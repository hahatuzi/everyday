# XGBoost算法：极限梯度提升树
  ## 公式：
  > 损失函数 + 正则化  
  > gain = 如果分裂前 - 分裂后   
  > 如果gain > 0 ,分裂后的树损失更小，考虑分裂，如果 gain < 0,损失更大，不建议分裂
  ## 当出发一下条件时停止分裂：
  - 达到最大深度
  - 叶子节点数量低于某个阈值
  - 所有节点再分裂不能降低损失

# 朴素贝叶斯算法
  ## 条件概率：P(A|B)
  ## 联合概率:P(A)*P(B)