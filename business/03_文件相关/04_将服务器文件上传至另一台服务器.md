```js
  async function () {
    console.log(detailInfo);
    let { fileName, url } = compareList.value[1];
    const res = await getFile(url);
    // 读取文件二进制数据信息
    const blob = res;
    // 将读取到的blob二进制数据作为一个新的File对象上传至另一个服务器端口
    const file = new File([blob], fileName, {
      type: blob.type,
    });
    const query = new FormData();
    query.append("file", file);
    // const { result } = await upload(import.meta.env.VITE_APP_API_URL + "/s/uploads", query);
    const result = await uploadUAVFile(import.meta.env.VITE_APP_UAV_URL + "/common/external/upload", query);
    detailForm.eventImages = result.url;
    submitForm();
  }
```