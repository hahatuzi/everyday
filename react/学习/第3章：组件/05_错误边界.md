# 错误边界：
  ### 1.错误边界的作用
  -  (1)捕获子组件树中的JavaScript错误（渲染、生命周期、事件回调中的错误），并显示备用 UI（避免整个应用崩溃）。
  - 只能捕获子组件的错误，不能捕获自身、异步代码（如 setTimeout）、事件回调中的错误。
  ### 2.错误边界无法捕获的情形：
  - (1)事件处理（了解更多）
  - (2)异步代码（例如 **setTimeout** 或 **requestAnimationFrame** 回调函数）
  - (3)服务端渲染
  - (4)它自身抛出来的错误（并非它的子组件）